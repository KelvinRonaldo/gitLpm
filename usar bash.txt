GIT
STAGED = prontos para serem commitados
git status ? status do repositório atual
git init ? tornar repositorio atual em um repositorio git
git add "nome" ? adicionar arquivo ao rastrio do git
?? coloca arquivo na lista para ser comitado
sequencia de comando ? add, commit
git ls-files? mostra apenas arquivos restreados
git add -A ? add todos arquivos dorepositorio ao
git log ? mostra todas as alterações
git log -p ? mostra alterações detalhadamente
q ? sai do resultado do log
git remote ? ver ropositorio remoto
git remote add 'origin' "link" ? define qual é o seu repositorio remoto
		? pode ser qualquer nome, mas origin é o recomendado
git push origin master ? dar push no do meu 'origin' para o 'master' do remoto
git pull origin master ? dar pull no do meu 'origin' para o 'master' do remoto
git config http.sslVerify false ? não fazer verificacção de ssl (desabilita criptografia)
git config https.sslVerify false ? "

LINUX
ls ? mostra conteúdo do repositorio
ls -l ? mostra conteúdo detalhado
ls -a ? mostra itens ocultos também
pwd ? mostra pasta atual
